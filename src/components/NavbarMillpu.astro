---
const pathname = Astro.url.pathname;
const navLinks = [
    { name: "Arequipa", href: "/arequipa" },
    { name: "Puno", href: "/puno" },
    { name: "Cusco", href: "/cusco" },
    { name: "Ayacucho", href: "/ayacucho" },
    { name: "Selva Central", href: "/selva-central" },
    { name: "Chachapoyas", href: "/chachapoyas" },
    { name: "Tarapoto", href: "/tarapoto" },
    { name: "Contáctanos", href: "/contacts" }
];
---
<nav id="navbar" class="w-full bg-black/10 backdrop-blur-sm py-4 px-6 fixed top-0 left-0 z-50 transition-all duration-500 border-b border-white/5">
    <div class="container mx-auto px-4 lg:px-8 flex justify-between items-center">
        <div class="w-auto">
            <a href="/" aria-label="Inicio">
                <img 
                    src="/images/logomillpu.avif" 
                    alt="Logo" width="200" height="100"
                    class="h-12 lg:h-16 w-auto"
                >
            </a>
        </div>        

        <ul class="hidden xl:flex xl:space-x-8 text-base font-bold text-white items-center">
            {navLinks.map((link) => {
                const isActive = pathname === link.href || pathname.startsWith(link.href + '/') || (link.href !== '/' && pathname.startsWith('/tours' + link.href));
                return (
                    <li>
                        <a 
                            href={link.href} 
                            class={`transition pb-1 whitespace-nowrap uppercase tracking-wider ${isActive ? 'text-white border-b-2 border-primary' : 'text-gray-100 hover:text-white'}`}
                            aria-current={isActive ? 'page' : undefined}
                        >
                            {link.name}
                        </a>
                    </li>
                );
            })}
        </ul>

        <button id="menu-button" class="xl:hidden text-white text-3xl" aria-label="Abrir menú">
            <i class="fas fa-bars"></i>
        </button>
    </div>

    <div id="mobile-menu" class="hidden absolute bg-black/95 backdrop-blur-xl w-full top-full left-0 border-t border-white/10">
        <ul class="flex flex-col space-y-4 py-6 px-4 text-center text-base text-white">
            {navLinks.map((link) => {
                const isActive = pathname === link.href || pathname.startsWith(link.href + '/') || (link.href !== '/' && pathname.startsWith('/tours' + link.href));
                return (
                    <li>
                        <a 
                            href={link.href} 
                            class={`transition block py-2 ${isActive ? 'text-primary font-bold border-l-4 border-primary pl-2' : 'hover:text-gray-300'}`}
                            aria-current={isActive ? 'page' : undefined}
                        >
                            {link.name}
                        </a>
                    </li>
                );
            })}
        </ul>
    </div>
</nav>

<script>
    document.addEventListener('DOMContentLoaded', () => {
        const navbar = document.getElementById('navbar');
        const menuButton = document.getElementById('menu-button');
        const mobileMenu = document.getElementById('mobile-menu');
        
        // Handle background change on scroll
        const handleScroll = () => {
            if (window.scrollY > 50) {
                navbar?.classList.add('bg-black/70', 'backdrop-blur-lg', 'py-3', 'shadow-2xl', 'border-white/10');
                navbar?.classList.remove('bg-black/10', 'backdrop-blur-sm', 'py-4', 'border-white/5');
            } else {
                navbar?.classList.remove('bg-black/70', 'backdrop-blur-lg', 'py-3', 'shadow-2xl', 'border-white/10');
                navbar?.classList.add('bg-black/10', 'backdrop-blur-sm', 'py-4', 'border-white/5');
            }
        };

        window.addEventListener('scroll', handleScroll);
        handleScroll(); // Initial check

        if (menuButton && mobileMenu) {
            menuButton.addEventListener('click', () => {
                mobileMenu.classList.toggle('hidden');
            });
        }
    });
</script>
